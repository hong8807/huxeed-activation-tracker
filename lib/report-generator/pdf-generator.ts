/**
 * PDF 생성기 (Stub)
 * Adapted from vtrack-ppt-generator/src/pdf/generator.ts
 *
 * Note: PDF generation requires puppeteer which is not installed.
 * To enable PDF generation, install puppeteer and uncomment the implementation.
 */

/**
 * Generate PDF buffer from HTML string
 * Currently a stub - requires puppeteer to be installed
 */
export async function generatePdfBuffer(html: string): Promise<Buffer> {
  throw new Error(
    'PDF generation is not available. Install puppeteer to enable this feature: npm install puppeteer'
  );
}
